version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
    - gosec
    - misspell

linters-settings:
  gosec:
    excludes:
      - G101

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gosec
